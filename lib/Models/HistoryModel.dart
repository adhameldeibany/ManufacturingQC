final class HistoryModel{
  String TimeSubmitted, Phase, Barcode, Description, Accepted, Declined, Shift, total;
  HistoryModel(
      {
        required this.TimeSubmitted,
        required this.Phase,
        required this.Barcode,
        required this.Description,
        required this.Accepted,
        required this.Declined,
        required this.Shift,
        required this.total,
      }
      );
}